package top.dhc.mydb.backend.tbm;

/**
 * FieldCalRes 类用于存储字段计算的结果。
 * 该类包含两个属性：`left` 和 `right`，用于表示比较操作中的左右值。
 */
public class FieldCalRes {
    public long left;  // 存储比较操作中的左值。
    public long right;  // 存储比较操作中的右值。
}
